{
  "name": "user-api",
  "version": "1.0.0",
  "description": "REST API for user management built with native Node.js",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=setup.env index.js",
    "dev": "node --env-file=setup.env --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "rest",
    "api",
    "nodejs"
  ],
  "author": "Max",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "prettier": "^3.8.1"
  }
}
